# v1.0 功能完善修复计划

> **目标**: 补全功能，让 1.0 版本功能完善
> **当前状态**: 批次1（入库P0）已完成，批次2（通用组件+假功能）已完成
> **预计总工期**: 6天

---

## 阶段1：剩余P0缺陷修复（0.5天）✅ 已完成

### 1.1 库存页面 — INV-29 批量报废
- **问题**: 批量报废为模拟，未调真实API
- **修复**: 绑定 `POST /scraps` 接口（后端已有API）
- **文件**: `前端代码/src/pages/inventory/InventoryList.tsx`

### 1.2 库存盘点 — ST-01 进行中统计
- **问题**: 硬编码 `const inProgress = 0`
- **修复**: 按 `status === 'in_progress'` 过滤统计
- **文件**: `前端代码/src/pages/inventory/Stocktaking.tsx`

---

## 阶段2：跨页面共性 — 封装通用组件（1天）✅ 已完成

### 2.1 提取通用Modal组件
- **文件**: `前端代码/src/components/ui/Modal.tsx`

### 2.2 提取通用ConfirmDialog组件
- **文件**: `前端代码/src/components/ui/ConfirmDialog.tsx`

### 2.3 提取通用Pagination组件
- **文件**: `前端代码/src/components/ui/Pagination.tsx`

### 2.4 提取 usePagination hook
- **文件**: `前端代码/src/hooks/usePagination.ts`

### 2.5 提取 useUrlParams hook
- **文件**: `前端代码/src/hooks/useUrlParams.ts`

---

## 阶段3：跨页面改造 — 17个列表页统一升级（2.5天）⏳ 待执行

按页面优先级依次改造：

| 优先级 | 页面 | 改造内容 |
|:---:|:---|:---|
| P0 | 库存列表 | 后端分页 + URL同步 + 替换前端筛选 |
| P0 | 出库记录 | 后端分页 + URL同步 |
| P1 | 物料管理 | 后端分页 + URL同步 |
| P1 | 供应商 | 后端分页 + URL同步 |
| P1 | 用户管理 | 后端分页 + URL同步 |
| P1 | 分类管理 | 后端分页 + URL同步 |
| P1 | 库位管理 | 后端分页 + URL同步 |
| P2 | 项目/BOM | 后端分页 + URL同步 |
| P2 | 预警/日志 | 后端分页 + URL同步 |
| P2 | 对账/成本 | 后端分页 + URL同步 |

每个页面的改造步骤：
1. 替换本地 `filteredData.slice` 为 `usePagination` hook
2. 筛选条件改为传给后端API
3. 筛选 state 用 `useUrlParams` 同步到URL
4. 分页UI替换为 `Pagination` 组件
5. 删除操作替换为 `ConfirmDialog`

---

## 阶段4：假功能补全（1.5天）✅ 已完成

### 4.1 打印操作人
- **文件**: `前端代码/src/pages/inbound/Inbound.tsx`

### 4.2 扫码入库（Inbound）
- **文件**: `前端代码/src/pages/inbound/Inbound.tsx`

### 4.3 批量导入（Inbound）
- **文件**: `前端代码/src/pages/inbound/Inbound.tsx`

### 4.4 导出功能
- **文件**: `前端代码/src/pages/inbound/Inbound.tsx`

---

## 阶段5：缺失页面评估（按需，不计入本次计划）

以下页面后端有API但前端无页面，用户确认后单独排期：
- 退库管理 (`returns-v1.1.ts`)
- 报废管理 (`scraps-v1.1.ts`)
- 调拨管理 (`transfers-v1.1.ts`)
- 采购订单管理 (`purchase-orders-v1.1.ts`，无菜单入口）

---

## 验证方式

1. **TypeScript编译**: 每阶段完成后 `tsc --noEmit`
2. **功能验证**: 启动前后端，在浏览器中测试核心流程
3. **E2E**: 不破坏已有通过的测试
4. **工作日志**: 每批次完成后更新 `.claude/SESSION-B-WORKLOG.md`

---

## 执行顺序建议

考虑到"尽快看到效果"的原则，建议按以下顺序执行：

1. **先做阶段1**（剩余P0，0.5天）→ 消除所有阻塞性缺陷 ✅
2. **再做阶段2**（封装通用组件，1天）→ 为后续改造打基础 ✅
3. **再做阶段4**（假功能补全，1.5天）→ 用户能感知到的功能完善 ✅
4. **最后阶段3**（跨页面改造，2.5天）→ 工程量大但用户感知较弱 ⏳

或者如果用户希望"先解决最影响体验的"，可以：阶段1 → 阶段4 → 阶段2 → 阶段3。
