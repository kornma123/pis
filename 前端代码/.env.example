# 前端环境变量示例。复制为 .env 后按需修改：cp .env.example .env
# ⚠️ 前端 .env 只放「公开的」构建期变量（会被打进产物、对用户可见）。
#    绝不要把 JWT_SECRET 或任何后端密钥放进前端 .env（历史上误放过并随公开仓库泄露）。

# 后端 API 基址（缺省时代码回退到 http://localhost:3001/api/v1，见 src/api/request.ts）
VITE_API_BASE_URL=http://localhost:3001/api/v1
