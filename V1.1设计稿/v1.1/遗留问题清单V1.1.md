# COREONE v2.2 遗留问题清单 V1.1

> **创建日期**: 2026-04-22  
> **来源**: 基于 `验收结果汇总V1.0.md` 中“遗留事项”章节提取。  
> **说明**: 以下问题已在 V1.0 验收中被标记为“后续迭代处理”，不影响本次上线，但建议纳入下一版本修复计划。

| ID | 问题描述 | 涉及文件/范围 | 建议修复版本 |
|:---|:---|:---|:---|
| LEGACY-01 | **页面内嵌样式迁移**：多个页面（如 `inventory-list.html`、`outbound.html`、`stocktaking.html`）中仍存在大量 `<style>` 标签，未完全迁移至共享样式表 `styles.css`。 | `pages/*.html` | V2.3 |
| LEGACY-02 | **非语义化按钮改造**：部分交互元素（如 `.stat-card`、`.category-row`）仍使用 `<div onclick>` 而非 `<button>`，影响键盘导航和无障碍访问。 | `categories.html`、`inventory-list.html` | V2.3 |
| LEGACY-03 | **文件命名规范化**：部分文件未严格遵循“小写字母+连字符”规范（当前实际已基本符合，但作为预防性提醒）。 | 全局 | V2.3 |

---