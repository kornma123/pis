# 新仓迁移差异与恢复说明（旧 master 43 个已合并提交缺失）

> 日期：2026-07-21 · 编制：Mac 车道 Claude · 状态：**待 PM 拍板 + Windows 车道执行恢复**
> 读者：PM、Windows 车道执行者。本文不含任何代码变更，只是盘点与建议。

## 0. 一句话结论

新仓 `kornma123/pis` 的 master 是 Windows 从**本地旧同步点**推上去的：它停在 PR #187（2026-07-15 前后）+ 1 个本地提交，**少了旧仓 master 上 #187 之后合并的 43 个提交（9 个 PR，其中 2 个是真生产代码修复）**。Mac 本地完好保存着旧 master 完整尖端（`87332ee3`)，已建恢复分支 `recovery/old-master-87332ee3` 并打包 bundle 放入车道 outbox，等 PM 拍板后由 Windows 合回。

## 1. 背景事实

| 事实 | 值 |
|---|---|
| 旧仓 | `Mazikorn/Coreone-...PSI...` —— 账号被 GitHub suspended，整仓 404 不可访问 |
| 新仓 | `kornma123/pis`,Windows 已推全量分支 + master |
| 新 master 尖端 | `1d849b08` docs(governance): add local issue guidance |
| 旧 master 尖端（Mac 本地保有） | `87332ee3` Merge PR #202 |
| 两版 master 分叉点 | PR #187 合并之后 |
| 新 master 独有 | 仅 1 个提交：`1d849b08` |
| 旧 master 独有（=新仓缺失） | **43 个提交** |

## 2. 缺失清单：9 个已合并 PR（43 提交）

按旧 master 上的合并顺序，新 master 缺失以下已合并 PR:

| PR | 内容 | 性质 | 缺失影响 |
|---|---|---|---|
| #190 | 整体产品总 PRD v1.3 入仓 | 文档 | 新 master 上 `docs/COREONE-PRD-整体产品-2026-07-14.md` **整文件不存在** |
| #193 | issue-184 历史来源契约 | 文档/口径 | 缺失 |
| #194 | issue-185 月差 mockup 硬化 | 文档/mockup | 缺失 |
| #196 | 单机私有工作机制收编入仓 | 治理 | **现行协作机制的法源文档缺失** |
| #197 | PRD 交付跨设备接线 | 治理/工具 | 缺失（含 `.claude/commands/coreone-prd.md`、`coreone-deliver-prd.md` 两个命令文件） |
| #202 | **前端写按钮判据迁 canAccess + fail-closed + lab_director 回归修复** | **生产代码修复** | 新 master 上前端写门回到旧白名单判据（已知有真回归） |
| #203 | **固定成本池拆出周期 profile 隔离** | **生产代码修复** | 新 master 上 hospital-cm 成本池隔离缺失 |
| #204 | 全局产品收官契约 | 文档/治理 | 缺失 |
| #205 | issue-182 roster 配置契约转写 | 文档/口径 | 缺失 |

> 其中 #202/#203 是**代码级修复**,#190/#196/#197 是**治理承重文档**。这 43 个提交全部经过当时的复核与合并流程，不是草稿。

## 3. 恢复资产（本次已备好）

1. **恢复分支**:`recovery/old-master-87332ee3` → 精确指向 `87332ee3`（旧 master 完整尖端，未做任何改动）。
2. **增量 bundle**:`~/coreone-kimi/outbox/old-master-tip-43commits-2026-07-21.bundle`
   - 内容：43 个缺失提交 + 本说明文档分支，前置依赖 = `origin/master`(Windows 已有，体积小）。
   - Windows 侧验证：`git bundle verify <file>`(应列出 recovery 与本说明两个 ref)。

## 4. 建议恢复路径（待 PM 拍板）

**推荐方案 A(merge，保真度最高）**:Windows 取 bundle → `git fetch <bundle> recovery/old-master-87332ee3:refs/heads/recovery/old-master` → 在新 master 上 `git merge recovery/old-master` → 异构复核冲突（预计集中在治理文档，代码冲突面小）→ 推新仓。43 提交原样保留，历史可溯。

**备选方案 B(cherry-pick 重排）**：只把 `1d849b08` cherry-pick 到 `87332ee3` 之上，以结果为 master 强推。历史更线性，但丢失旧合并拓扑，且属重写已发布历史，需 PM 明示授权。

> ⚠️ 纪律：Mac 车道不 push(PM 2026-07-21 拍板），恢复动作只能由 Windows 执行或 PM 另行授权。

## 5. 已核实"不缺"清单（防误判，勿重复抢救）

以下项目初看像缺失，逐项核实后确认**新 master 已有更权威版本或本地只是旧稿**:

| 项 | 核实结果 |
|---|---|
| #164 结算总额 16 院锚台账 | ✅ 新旧 master 均有 `docs/reports/COREONE-结算总额Candidate锚台账-16院-2026-07-13.md` 且**字节一致**；本地分支 `claude/great-gagarin-f9e7fb`、`docs/settlement-anchor-widening-164` 是过程稿，可弃 |
| #166/#167 PRD 地基校准 + ADR-005/006 退役 | ✅ master 的 PUBLIC 脱敏版**已含 E-1 勘误**;ADR-005/006 的 SUPERSEDED 头已在 master；本地分支 `pr167head`、`docs/prd-foundation-calibration-166`、`docs/cost-authority-adr-sync-166` 是过程稿 |
| 质量Loop 6 文档（2026-07-12) | ✅ master 上是 PR#154 收敛终版（更薄）;Mac 本地 6 份 untracked 是**收敛前旧草稿**,勿推 |
| 本地散文件 `docs/COREONE-PRD-整体产品-2026-07-14.md` | 是 v1.2 时代工作稿；旧 master 上有 v1.3(#190)，随 43 提交恢复即可，本地旧稿勿推 |

## 6. 本地独有项处置建议

| 项 | 内容 | 建议 |
|---|---|---|
| `docs/reports/` 2 份内部稿（校准 197 行/审计 164 行，含记名医院信息） | master 公开版的**脱敏前内部旧稿**,且不含 E-1 勘误 | **留本地、永不推仓**（脱敏红线）；以 master 公开版为准 |
| `.claude/CLAUDE.md`(CodeGraph 使用指引） | 全项目通用、无机器路径 | **建议正式入仓**（新旧 master 都没有） |
| `.claude/settings.json` | 本机 hook 配置 | 留本地 |
| `.stignore` / `my_agent/` | syncthing 配置 / 502 字节练手脚本 | 留本地，无入仓价值 |
| stash ×2(6 月 "pre-switch") | codex/abc 分支残留 | 可弃 |
| `codex/abc-productization-phase0-1-2026-06-15` 比其远端多 2 提交 | 6 月设计基线+session-log，历史价值低 | 留本地归档即可 |
| `backup/abc-phase0-pre-workflow-strip` | 备份性质分支 | 备份留本地，本就不该上仓 |
| ~50 个陈旧本地分支（全部 ahead=0) | 旧 PR 过程分支 | 迁移稳定后可批量清理（用 `scripts/gc-worktrees.cjs` 同款判据） |

## 7. 待办

- [ ] PM 拍板恢复方案（推荐 A)
- [ ] Windows 从 outbox 取 bundle 执行合并 + 异构复核 + 推新仓
- [ ] 恢复完成后：Mac 侧 `git fetch` 核对新 master 包含 `87332ee3`
- [ ] （可选）`.claude/CLAUDE.md` 单独小 PR 入仓
- [ ] （可选）清理 Mac 本地陈旧分支与 stash
