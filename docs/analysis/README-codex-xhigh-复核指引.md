# codex xhigh 远程复核指引 —— 成本口径讨论

> 本分支 `claude/cost-methodology-deepreview` 为远程 codex `xhigh` 深度复核准备。pull 完按下面跑。

## 要复核的东西（3 份文档 + 1 个脚本）

| 文件 | 是什么 |
|---|---|
| `docs/COREONE-成本口径讨论全过程实录-业务逻辑推演-2026-07-03.md` | **主目标**：从"抗体是不是物料"到"该花多少"方法论的**完整推演实录**（详细过程、每步理由、留下的开放问题）。§0 有给复核者的话，§12 是我们自己知道还没解决的靶子。 |
| `docs/COREONE-单切片成本口径-该花多少标准成本+贡献毛利-方法论固化-2026-07-03.md` | 蒸馏后的**结论固化**（①②③框架、贡献毛利、BOM 双时钟、PM 拍板）。 |
| `docs/analysis/cm_sim-贡献毛利端到端模拟-2026-07-03.py` | 讨论中"砸模型"用的端到端数值模拟，`python3` 直接跑（`seed=42` 确定性）。 |
| 代码锚点 | 实录 §附 列了 file:line（`cost-calculator.ts:580` 等），可只读核实。 |

## 建议的 codex xhigh 提示词（一次一个聚焦请求，防断流）

> ⚠️ 经验：xhigh 长审 + 大上下文易**断流**（`stream disconnected`）。**别一条塞满**——拆成下面几个聚焦请求，`codex exec` 起头 + `codex resume --last` 续问，一次只喂一个文件/一个问题。

**请求 1（业务逻辑主审）**：
```
codex exec -s read-only -c model_reasoning_effort=xhigh "读 docs/COREONE-成本口径讨论全过程实录-业务逻辑推演-2026-07-03.md（只读它）。这是一场病理实验室 PM 与 AI 的成本口径讨论实录。用最深推理砸业务逻辑：①①②③三档框架有没有逻辑缺陷/边界划不清/第四类量无处归？②贡献毛利判去留+固定成本不下沉，有没有反例给出错误去留信号（产能/合作形态/跨期）？③'标准成本'长期会不会系统性漂移、校准信号本身有没有偏？④从头到尾有没有哪步'把A问题答案错套到B问题'？逐条给：问题+严重度+改法。别客套。"
```

**请求 2（模拟脚本审，resume 续）**：
```
codex resume --last "现在读 docs/analysis/cm_sim-贡献毛利端到端模拟-2026-07-03.py（只读它）。砸它的假设与结论：收得率区间/涨价幅度/囤货规则/片价/扣率哪些不真实会扭曲结论？'死亡螺旋'换假设还成立吗？有没有它没模到但会翻盘的真实行为（退货/共用一瓶跨多院/批量复染/深切/代送工序集）？逐条给。"
```

**请求 3（开放问题深挖，resume 续）**：
```
codex resume --last "读实录 §12 的 8 个开放问题。挑你认为最被低估的 2-3 个深挖，给出具体的业务逻辑思路或反驳。另外：§12 有没有漏列的真问题？"
```

（如需核代码锚点，另起：`codex exec -s read-only -c model_reasoning_effort=high "只读 cost-calculator.ts 的 calculateMaterialCost(约580) 核实实录§附的公式描述"`——代码核实用 high 就够、别 xhigh。）

## 说明
本地已跑过：Claude 三视角红队 + codex 异构(gpt-5.5·high·7 处采纳) + 一线/工程/决策三视角面板；本地 codex xhigh 那一轮**断流未出结论**——这正是让远程 xhigh 补这一轮的原因。远程若也断流，降到 `high` + 逐文件即可。
