# COREONE 基础模块 — 实现任务拆分（供多会话并行）

> **日期**：2026-07-02 | **性质**：把 `docs/COREONE-账实复核与逐抗体成本-未决问题与关联文件-2026-07-02.md` 里**属于"前面基础模块"的未决项**拆成可并行、互不碰文件的独立线，供多个新会话同步启动。
> **配套必读**：设计基线 `docs/COREONE-账实复核与逐抗体成本-设计基线-2026-07-02.md`、未决清单（同上）、`CLAUDE.md`、`.claude/rules/coreone-guardrails.md`、`.claude/rules/pr-governance.md`、工作模型 `docs/工作模型-通用版-…` + `…-COREONE项目版-…`。

---

## 0 · 范围与边界（谁做什么）

- **另一会话已在做**：LIS 导入 + 财务对账（账实核对引擎 / 账实核对三页前端 / 收入侧拆分）+ 独立复核。**本拆分一律不碰**：`reconcile-*.ts`、`account-reconcile-v1.1.ts`（含 `classifyChargeItem`）、`前端代码/…/account-reconcile/*`、`statement-revenue.ts`、`partner-config.ts`。
- **本拆分负责**：这份 backlog 里坐在对账引擎**下面那一层**的**逐抗体成本基础数据 + 统一目录地基**（纯成本侧，与收入侧物理隔离）。
- **前提现状**：三阶段（#24 成本地基 → #27 核对引擎 → #30 三页前端）**已全合 master**（tip `0b662efe`），**零 open PR**。每条新线从 **origin/master** 出发、各开小 PR 合回。

### 已关闭 / 不需动的项
- **D1 分支拓扑合流**：✅ 已解决（收入侧+成本侧全在 master，无孤儿线新活）。无需处理。
- **C3 spawned 待办**：`task_f895c7a7`（admin 审计缺口）+ `task_e099f3ac`（替代料双计）经核实**均为误报**，无需修（见记忆 `coreone-bom-is-alternative-misnomer`、session-log 2026-07-02 审计收口）。
- **A4 特染混进免疫组化**：分类逻辑 `classifyChargeItem` 在**对账引擎**里 → 归对账会话；成本侧特染口径（盒价÷标称次数 `specialStainPerTestCost`）已在，本拆分不重复。

---

## 1 · 每条线都必须遵守（通用约束）

1. **从 master 出发**：确认 `git fetch origin`，分支基线 = 最新 `origin/master`（spawned 会话自带 fresh worktree；若不在 master 基线，按 `pr-governance.md` 从 `origin/master` 开分支）。
2. **工作模型四段**：① 先讨论/摊假设——把本线口径和"要 PM 拍/要真数据"的点先列出来给 PM，**别闷头写码**；② BDD/TDD 先行（红→绿）；③ 前端才走 mockup 先行（本三条线均后端/数据，无此步）；④ 收尾走独立第二视角复核（codex 异构引擎，见 `codex-cli-usage.md`）。
3. **金标零回归**：守 golden **¥13,152**（partner-revenue）+ **¥27,870**（hemujia-purelab）。改动后 `cd 后端代码/server && npx vitest run` 全绿再开 PR。
4. **PII 纪律**：读 `~/Downloads` 里的 xlsx **只取分析列**，严禁把姓名/出生日期/MRN/联系方式/临床诊断入库或写进种子；xlsx **不进仓**（抽成不可变种子 `.ts`，同 Phase 0 做法）。
5. **PR 治理**：PR body 按 `.github/pull_request_template.md`（base/依赖/单独可合/验证/守 golden）；开/合后更新 `pr-governance.md` 看板 + `session-log.md`。
6. **git 纪律**：只 `git add` 本线目标文件，**别 `git add -A`**（`.claude/skills-runtime/` 会被误纳）；**绝不改动其他会话 worktree 的 session-log**，若改到共享文件留未暂存由其树自提。
7. **物理隔离**：成本侧只加**新表/新 util/新路由**，不写收入侧、不改对账引擎。`DatabaseManager.ts` 是共享文件——各线追加各自表块/改各自 seed 区，撞车时后合方 `git merge origin/master` 消解。

---

## 2 · 线 A — 逐抗体成本·抗体名称映射与缺价补全（未决 A1 + A3）

- **目标**：让 LIS 只给"抗体名"时也能对上台账的**价 + 剂型**，并把台账真缺的抗体清出来。
- **A3 剂型映射**：台账 `antibodies` 表已有 `UNIQUE(name, form)`；剂型（原液/即用）影响每片成本约 6 倍。建 LIS 抗体名 → 台账（价, 剂型）映射；LIS 缺剂型时按台账映射补。
- **A1 缺价 10 种**：LIS 用到但台账没有的 10 种（Ecad / Melan A / PD-1 / Vimentin / cathepsinK / cyclinD1 / GPNMB / TROP-2 / SMARCA4 / HP）。第一步**先查是否别名/异名/规格差异**（读 `~/Downloads/免疫组化相关耗材2025年.xlsx` sheet「2025 (2)」核）→ 能映射的建别名映射；**真缺的**行级标"成本缺价·毛利待定"（降级路径 `fallbackAveragePrimary` 已存在），并**产出一张缺价清单交 PM 补采购价**。
- **独占文件**：`后端代码/server/src/utils/antibody-catalog.ts`、`antibody-cost.ts`，可新建 `antibody-name-map.ts`；若加别名种子，追加到 `DatabaseManager.ts` 清晰标注区（幂等 `INSERT OR IGNORE`）。
- **不碰**：对账引擎、收入侧、前端。
- **验收**：TDD 覆盖（剂型映射 / 别名命中 / 真缺降级标注）；`vitest run` 全绿；golden 零回归；PR body + 缺价清单交 PM。
- **PM 待解锁**：真缺抗体的采购价（会话产出清单后由你补）。

---

## 3 · 线 D — 统一检测项目目录（映射层）（未决 D2）

- **人话**：同一检测项目在四处有四套叫法——`projects.code` / 国标收费码 / LIS 名 / 对账单码——**没有硬映射**、全靠关键词猜。建一张"这些叫法指同一个项目"的**对照表（目录层，不合并现有表）**，作为口径地基。
- **第一步 = 先摊口径给 PM 拍**（工作模型 discussion-first）：读 `docs/COREONE-T1真数据手核+基础线收入线合流方案-2026-07-01.md` 的目录层设计 → 出**最小 schema 草案**（对照表字段 + 用哪几套码做主键/别名）+ 说清它先"并存"、后续被 A/B 分类逻辑吸收 → **给 PM 看懂再落码**。
- **落码**：新表（如 `project_catalog` / `code_mappings`）+ 幂等 seed（从现有 projects/国标 81 码/对账单码/LIS 名抽映射）+ 查询 util `project-catalog.ts`（按任一叫法查规范项目）。**不改现有分类逻辑**（先并存）。
- **独占文件**：新建 `后端代码/server/src/utils/project-catalog.ts` + `DatabaseManager.ts` 新表块（append）+ 可选只读路由。
- **不碰**：对账引擎的 `classifyChargeItem`（先并存，后续那条会话决定是否消费本目录）。
- **验收**：设计草案先过 PM；TDD 覆盖（按四套叫法各查得同一项目 / 未命中兜底）；`vitest run` 全绿；golden 零回归。

---

## 4 · 线 F — 逐抗体成本·G2 弱锚校准 + 承重墙口径（未决 B4 + B3）

- **B3 承重墙口径（现在能做）**：把"国标 36/105 比例套和睦家溢价单价 = **政策分摊、非真实制片价值证明**"这条写成**文档 + 敏感性区间**（诊断锚 105 → 本地协商值区间），不当事实售价证明。产出：口径文档 + 可配区间参数。
- **B4 弱锚校准（机制能做 / 真值待 PM）**：`ihc_cost_params` 里 `labor_per_slide`/`equipment_per_slide` 是 G2 估（source='G2估' confidence='粗估'，remark 已标"待康湾真实工资校准"）。本线：① 保证接口/派生**诚实透出「G2估·待校准」**不冒充精确；② 建"用真实工资/折旧/房租回归算出并写回参数"的**输入路径 + 留痕**（source/confidence 更新）；③ **产出"需要康湾哪些数据"的清单交 PM**。真值待 PM 补。
- **独占文件**：`DatabaseManager.ts` 的 `ihc_cost_params` seed 区（改值/加参数位）、`antibody-cost.ts`（敏感性 band）、`docs/`（B3 口径文档）。
- **不碰**：前端（逐抗体成本页归对账/前端会话）——本线只做后端参数 + 文档；诚实透出的字段留给前端消费。
- **验收**：TDD 覆盖（G2 值走 source/confidence 留痕 / 校准写回覆盖但留痕 / 敏感性区间边界）；`vitest run` 全绿；golden 零回归。
- **PM 待解锁**：康湾真实工资 / 折旧 / 房租（会话产出所需清单后由你补）。

---

## 5 · PM 待解锁输入汇总（不是会话，是你/业务给的数据或拍板）

| 编号 | 需要你提供 | 关联线 |
|---|---|---|
| A1 | 真缺抗体的采购价（会话先产出缺价清单） | A |
| A2 | 3 家医院扣率（实收=收费×扣率，缺则该院实收算不对） | （对账会话用；非本拆分实现，仅提醒补数据） |
| B4 | 康湾真实工资 / 折旧 / 房租（会话先产出所需清单） | F |
| D2 | 对照表口径拍板（会话先出最小 schema 草案） | D |

---

## 6 · 排除清单（本拆分明确不做，归另一会话/后续）

- **B 核对引擎**（A4 特染分离 → B1 组织学差异定义）
- **C 前端三页真实现**（C1 下钻明细/导出补收单/五态）
- **E 收入侧拆分逐院验证**（B2 36/105 通用 vs 逐院）
- **G 独立复核**（C2，你后续在对账会话起）

*本拆分与设计基线、未决清单配套。每条线开工先读通用约束（§1）+ 本线卡 + 设计基线 + 未决清单。*
