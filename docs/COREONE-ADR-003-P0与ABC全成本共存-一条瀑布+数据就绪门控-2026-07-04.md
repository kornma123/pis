> ⚠️ **状态：PARTIAL（2026-07-14）**。本 ADR 只保留三项现行约束：① 贡献毛利与 ABC 全成本放在**一条瀑布**里解释，避免两个“利润数”并列打架；② ABC 全成本**不驱动具名账户红色、排名、砍院或谈价**；③ 三件套未齐时按**数据就绪门控**降级，不用空的新口径遮住旧视图。本文原“把 ABC 的具名账户 KPI/排序/颜色/状态全部切给贡献毛利”以及“贡献毛利自动发可留/需谈价/停止候选/需限量”的部分已退役，**不得据此重新实现自动点名、自动排名或自动清单**。
>
> 现行顶层权威＝`COREONE-P0-PM待拍清单-Q1toQ11收官-2026-07-04.md` §B 收敛终稿 + `COREONE-ADR-008-组合体检覆盖倍数-固定成本池口径-2026-07-07.md`：①组合体检不点名账户；②人看对照表将绝对贡献额、率、同账户趋势并列，系统不自动排名/打分/生成清单。详细机制改版由 Issue #180 承接。

# ADR-003 · P0 贡献毛利与现有 ABC 全成本毛利的共存

> **日期**：2026-07-04 | **状态**：PARTIAL（2026-07-14；保留瀑布/ABC 非决策信号/数据就绪门控，退役具名账户自动裁决）| **性质**：hard-to-reverse（决定两个“利润数”的关系与就绪降级），**动到已上线的 `/hospital-pnl`**。
> **关联**：`partner-pnl-service.ts:190`（ABC grossMargin）；`HospitalPnLDashboard.tsx`（消费方）；P0 spec；ADR-001/002；方法论固化 §3（贡献毛利判去留·死亡螺旋）+ codex HIGH1（分 lane）。

## 背景（Q5·已核实）

- `/hospital-pnl` 现在展示 `partner-pnl-service.ts:190` 的 `grossMargin = 实验室收入 − ABC 全成本`；`costTotal` 取 `outbound_abc_details.total_cost`，`cost-calculator.ts:379` = material+labor+equipment+qc+indirect（**含整块固定成本**）。
- P0 = `实验室收入 − 可避免材料`（不含工时/设备/间接）。**分子同、分母差一整块固定成本 → P0 ≥ ABC 恒成立。**
- 边际院必然出现 **ABC<0（全成本亏）但 P0>0（边际赚）** → 同一家院 ABC 说"砍"、P0 说"留" → 财务两个都不信 → **adoption 红线**。且这正是方法论警告的**死亡螺旋**（按全成本砍边际院 → 固定成本转嫁 → 更多院翻红 → 继续砍）。
- **现状代码把去留信号全长在 ABC 上**：worst-first 排序 `:41`、"负毛利 N 家"KPI `:112/:67`、负数染红 `:167`、负毛利筛查栏 `:207`、页脚"决策以院级毛利为准"`:230`。

## 决策

**不是“哪个数对”——两个数各答不同问题：内圈贡献毛利提供边际经济信息，中圈全成本观察长期成本结构。问题是两个“利润数”并列打架毁信任。当前解法 = 一条瀑布 + ABC 不驱动具名账户决策信号 + 数据就绪门控；具名账户是否行动由两层框架交给人判断。**

> **当前读取边界**：下列第 1、3、6 项继续现行；第 2 项只保留“ABC 不驱动具名账户决策信号”，不再把这些信号切给贡献毛利；第 4、5 项的具名账户裁决/状态方案已退役。正文保留用于解释历史决策，不构成当前实现命令。

**1｜一条瀑布，不是两个并列数**：`/hospital-pnl` 单卡显示一条钱的瀑布：
```
实验室收入 → −可避免材料 = 贡献毛利（主·供人判断，不发自动状态）
            [默认收起，一键展开] → −工时/设备/分摊 = 全成本毛利（长期趋势·不作去留依据）
```

**2｜PARTIAL · 只保留 ABC 不驱动具名账户决策信号**：`lossCount`/"负毛利 N 家"KPI、列表排序键、红色语义、负毛利筛查栏和页脚口径**不得再由 ABC `grossMargin` 驱动**；ABC 永不参与具名账户排名，也不用“亏损红”。原“这些信号全部切到贡献毛利”的迁移命令已退役——现行两层框架同样禁止贡献毛利自动发具名账户红色、排名、状态或清单。

**3｜全成本降级为"看得见但不驱动"（面板 trust#4·答 Q2）**：同卡**默认收起、一键展开即完整**（**不移到另一个视图**——移走=坐实"藏亏损"）。展开态：中性灰/琥珀色（非 rose 红）、顶部硬门禁横幅"工时/设备/间接为分摊估值·不得用于去留/砍院/谈判·仅供成本结构长期观察"。标签用"**不作去留依据·仅长期趋势**"，**不用"估/不准"**（面板 trust#3：'估'触发不信并传染整表，'不作依据'只触发不用）。

**4｜历史呈现方案 · 已退役**：原计划在贡献毛利“可留”旁展示因果锚与“砍这家会怎样”的反事实。两层框架不再自动发“可留”等具名裁决词；“固定成本不能靠砍单院省掉、避免死亡螺旋”的洞见仍保留为组合层/人工判断的解释原则。

**5｜PARTIAL · 保留产能约束，退役自动状态**：`贡献毛利>0` 只是边际经济信息，**不等于系统背书“该留”**；产能利用率继续进入组合体检。原 `需限量` 自动状态及“按单位瓶颈资源贡献毛利决定具名账户去留”的命令已退役；处方层若需使用产能费，必须等产能实测与相应门控就绪后由人决策。

**6｜数据就绪门控·逐院逐月渐进切换（面板 trust#5/deathspiral#3·最关键，别一步到位翻口径）**：
- P0 贡献毛利 lane **现在没真数**（spec §7/§10.F：`case_revenue`/`lis_case_markers`/`antibodies` 空）。上线若"贡献毛利空 + 全成本藏" = 用空口径顶掉能用口径 → 财务一次崩。
- 规则：某院某月**三件套未齐** → 主瀑布显式显示"贡献毛利：需导入对账单+抗体清单后生成"（给补数动作，非空白），此期间**全成本不折叠、正常展示但标"过渡口径·数据补齐后由贡献毛利替代"**；**只有三件套齐、贡献毛利真出数** → 才切"贡献毛利主 + 全成本折叠"目标态。财务永不撞见"新的空 + 旧的被藏"。
- 加真实性门禁：`outbound_abc_details` 真实归集率低于阈值 → 全成本行只出"数据未就绪"占位、不出假数字（防假负数锚定）。

## 后果

- ✅ 财务在主视图只有**一个主要的边际经济数**（贡献毛利），但系统不把它自动翻译成具名账户去留/谈价裁决；无“两个利润数打架”、无假负数警报。
- ✅ 死亡螺旋在信号层被切断：ABC 不喂红/排名/砍，贡献毛利也不自动点名；固定成本不能靠砍单院省掉的反事实继续作为人工判断原则。
- ✅ 全成本一键可达、完整 → 挡"藏亏损"指控；渐进切换 → 挡"空窗期"信任崩。
- ⚠️ **历史迁移方案已退役**：不得再把 `marginColor`/排序/KPI/筛查栏/状态从 `grossMargin` 机械切给贡献毛利；当前两层实现与后续机制以活代码、ADR-008 和 #180 为准。`grossMargin` 数本身不改，只降级其呈现与信号角色。
- ⚠️ 产能利用率/产能费只有在实测与相应就绪门通过后才能进入组合体检或处方层，不得据占位数据自动裁决具名账户。

## 备选（未采纳）

- **退役 ABC grossMargin / `partner-pnl`**：否——全成本有长期定价/撤线的正当用途（中圈），且退役已上线消费方（`/hospital-pnl`）风险大（见新旧重叠处置清单 #4：partner-pnl 不可退役）。
- **全成本移到另一个视图**：否——坐实"藏亏损"，比并列更伤信任。
- **一步到位把头条从 ABC 翻成 P0**：否——P0 无真数时 = 空窗期信任崩。

## 变更记录
- 2026-07-04 采纳：Q5 拷问 + 三视角对抗面板（`wf_c8309493`，trust/deathspiral/migration 均"要补强·方向对"）补强后定稿。
- 2026-07-14 标 PARTIAL：保留一条瀑布、ABC 不驱动具名账户决策信号、数据就绪门控；退役“CM 接管具名账户 KPI/排序/颜色/状态”及自动去留/谈价/限量裁决，顶层统一到收官页 §B + ADR-008；详细机制由 #180 承接。
