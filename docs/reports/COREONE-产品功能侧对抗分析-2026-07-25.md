# COREONE 产品功能侧对抗分析（只做减法）— 2026-07-25

> 性质：继治理侧分析之后的**产品功能侧**对抗审。范围=当前 master 上**已有功能**的真实性、一致性与去留；**不提出任何新功能**，只给删除、修正、改名、隐藏类建议。
> 取证方式：前端 300 个 src 文件中 298 个与 master 逐字节相同（本地直读=master 实读）；仅 `route-registry.ts`、`HospitalCmDashboard.tsx` 两个文件从 master SHA `bc4a9fd8` 原文拉取。后端路由文件上轮已全部验证 SAME。
> 证据标签：CODE_CONFIRMED=当前 master 活代码确认；AUTHORITY_DEFINED=权威文档；LIVE_VERIFIED=GitHub 现场；INFERENCE=推断（已注明）。

---

## 0. 一句话结论

当前 master 的产品功能面存在**一个伪造数字的硬伤（PF1）**、**一整面与已拍范围矛盾的退役货架（PF2）**和**两处命名/定位混乱（PF3/PF4）**；它们全是减法式修复，不需要任何新功能。

---

## 1. 功能侧硬问题（按严重性排序）

### PF1 【伪造数字·最严重】物料成本分析页的"同期变化"列在真值为 null 时显示**随机数**

- **完整证据链（全部 CODE_CONFIRMED at master）**：
  - 导航 active：`/cost-analysis` 物料成本分析（route-registry.master.ts:163）；
  - 路由：`App.tsx:79` → `pages/report/CostAnalysis`；
  - 表格：`MaterialCostTable.tsx:75` —— `const changeValue = m.changeRate ?? Math.round(Math.random() * 30 - 15)`；
  - `ProjectCostTable.tsx:133` —— `const changeValue = p.changeRate ?? Math.round(Math.random() * 20 - 10)`。
- **后果**：后端按诚实口径返回 `changeRate: null`（LOC-030 已确认），前端却给每个物料/项目**编造一个 ±15%/±10% 的随机环比**，每次刷新数字还会变。这是碰钱展示面上的**伪造精确**，正是整个项目诚实体系（unknown≠0、算不出显"不可计算"）要杀的东西——而且它藏在"已经修过"的叙事里。
- **更尖锐的矛盾（两份权威文档都声称它不存在）**：
  - PRD v1.6 §4.1 诚实缺口行：「成本报表当前把同期变化保持为 `null/不可计算`」——**与代码事实相反**；
  - 本地 Issue 指导 LOC-030：「Mac C8 的'前端 Math.random 兜底'在 d144 未复现……因此不得按旧触发开 bug」——**校准结论错误**，随机兜底就在当前 master。
  - 也就是说：文档不仅没防住问题，还给了"已确认无此问题"的假凭证。#31（LOC-030，OPEN）的触发前提需要更正。
- **减法修复**（不加功能）：删除两处 `?? Math.round(...)` 兜底，`null` 一律渲染"不可计算"；同步更正 #31 正文与 PRD §4.1 的描述。这是行级修改，但按契约仍须走 Issue + preflight（可并入 #31 重述后派工）。

### PF2 【退役面仍在货架】master 导航仍暴露 15 个已拍退役的产品入口

- **CODE_CONFIRMED（route-registry.master.ts，全部 status:'active'）**：
  - ABC 报表面 5 条：`/abc/dashboard`、`/abc/slide-cost`、`/abc/profitability`、`/abc/alerts`、`/abc/audit`；
  - ABC 配置面 7 条：`/abc/activity-centers`、`/abc/cost-drivers`、`/abc/cost-pools`、`/abc/fee-mappings`、`/abc/budgets`、`/abc/quality-costs`、`/abc/quarterly-adjustment`；
  - 工时/设备/间接 3 条：`/equipment`、`/labor-times`、`/indirect-costs`。
- **矛盾**：2026-07-24 PM FINAL（ABC 退役边界 §1/§5）要求"导航/Dashboard/可达路由不再暴露"这些面；但移除它们的 PR #74 仍是 Draft、等 R2、且 owner 块自报 lint baseline 红。**在 #74 合并之前，任何人打开系统看到的就是与已拍范围直接矛盾的产品**。这不怪 #74——但意味着产品面的"错误状态"已经而且会持续到 R2 瓶颈解开。
- **附加**：6 条 headless 路由（`/equipment/depreciation`、`/abc/fee-comparison`、`/abc/supplier-costs`、`/abc/trend`、`/abc/variance`、`/abc/model-validation`）的处置理由写的是"PM 待拍归位，due 2026-10-07"——退役决定后这些理由已过期，它们属于退役面，应随 #69/#70 移除或关闭，而不是"归位"。
- **减法修复**：唯一出口就是推 #74 过非作者 R2 并合并；headless 6 条的 owner/reason 随 #69 改写（不新增路由、不新增页面）。

### PF3 【命名/定位混乱】导航里仍有一个叫"消耗对账"的页，而 PRD 说消耗对账域"整域已删"

- **CODE_CONFIRMED**：`/reconciliation`「消耗对账」active（registry:162），页面含 按项目对账/按物料汇总/按病理号/修正日志 四个 tab + LIS 导入 + 修正 approve/reject（useReconciliationPage.ts:122–316 调 `/reconciliation/*` API 家族）。
- **矛盾**：PRD §4.8（v1.5 原文，v1.6 未改）写「消耗对账(depletion)整域已删（领用≠使用）」。实际删掉的是 depletion 数据域，这个**页面和 `/reconciliation` API 家族留了下来**——它实际是账实修正工作台的雏形，却顶着"已删域"的名字，还与 `/account-reconcile`「账实核对」并存。两个相邻名字，用户和接手者都无法区分（本工具第一轮就把它当"未扫描页面"放过）。
- **减法修复**（需 PM 先确认定位）：改导航标签使其名实相符（例如并入"账实"语义），或明确与 `/account-reconcile` 的分工后只留一个入口。纯文案/导航修改。

### PF4 【测试台进正式货架 + 未登录全显】

- **CODE_CONFIRMED**：`/import-console`「导入测试台」active（registry:174）；`AppSidebar.tsx:38` —— **未登录（role=null）时侧栏显示全部 active 项**，含上述 15 条退役面和测试台；登录后 `permModule:null` 的项（partner-config/lis-cases/import-console/import-wizard）对菜单可见性的过滤语义依赖 `getAccessiblePaths()`（INFERENCE：null 模块大概率=全角色可见，未深挖验证）。
- **问题**：一个名字自称"测试台"的页面出现在正式主导航；未登录态把全部菜单（含 ABC 面）亮给未认证访客。后端有 403 兜底，这不构成越权写，但与"最小必要 + 内部工具"定位相悖，也把退役面继续外显。
- **减法修复**：测试台改名或收入「财务月度导入」入口；评估未登录全显策略（可改为未登录只显登录页）；`permModule:null` 项逐条确认是否该挂模块（INFERENCE 部分须先验证 `getAccessiblePaths` 语义再动手）。

### PF5 【体验债仍在】3 处原生 `window.confirm`

- **CODE_CONFIRMED**：`pages/reconciliation/components/LogListTab.tsx`、`pages/master/hooks/useLocationsPage.ts`、`pages/partner-config/PartnerConfigPage.tsx`。删除/驳回类动作用浏览器原生确认框，与全站自定义弹窗不一致（5 月清单 C-04 的债，至今仍真）。
- **减法修复**：换统一确认弹窗，R0/R1 级。

---

## 2. 做对了的（正面清单，防止减法误伤）

- **hospital-cm 认账水印 fail-closed**：master 版 `HospitalCmDashboard.tsx:52,95–104` —— 仅后端明确 `ratified===true` 才免水印，缺席/未认账恒显示、不可折叠 [CODE_CONFIRMED]。这是全站诚实设计的样板，PF1 的修复应照此标准。
- **route-registry 单一注册表 + status（active/headless/deprecated）+ 快照锁**：导航有机器治理，PF2 的问题不是机制缺失，而是退役决定尚未执行到注册表 [CODE_CONFIRMED]。
- **`/hospital-pnl` 旧看板已 deprecated 重定向 `/hospital-cm`，组件已删**（registry:221）——历史减法的好先例。
- **库存 schema CHECK、hospital-cm 403 门、未就绪零数值**（上轮已证）。

---

## 3. 减法清单（Now / Next；无任何新功能）

**Now（本周可动）**
1. **PF1**：删除两处随机兜底 → null 渲染"不可计算"；同时更正 #31 正文（"未复现"结论错误）与 PRD §4.1 描述。责任角色：PM 先确认 #31 重述，再派一个串行 owner；准入条件=Issue 重述+preflight+owned=report 两个表格组件。
2. **PF3**：PM 一句话确认 `/reconciliation` 页定位 → 改导航标签或合并入口。纯文案，R0。
3. **PF5**：3 处原生 confirm 换统一弹窗，R0/R1。

**Next（依赖他人/他票）**
4. **PF2**：给 #74 指定非作者 R2 reviewer（与上轮结论相同——R2 瓶颈同时卡着治理面和产品面）；headless 6 条 ABC/设备路由的 owner/reason 随 #69 改写为"随退役移除"。
5. **PF4**：先验证 `getAccessiblePaths()` 对 `permModule:null` 的语义（INFERENCE 待证），再决定测试台收敛与未登录全显的处置；不凭猜测改导航。

---

## 4. 边界声明

- 本轮严格只读：未改代码/仓库/GitHub；PF1–PF5 的修复建议全部需要按契约走 Issue + preflight + 独立复核 + PM 合并门，本文不授权实施。
- PF1、PF2、PF3、PF5 为 CODE_CONFIRMED 的当前 master 事实；PF4 中含一条 INFERENCE（`permModule:null` 的菜单过滤语义），已标明须先验证。
- 本轮刻意**不提出任何新功能**；所有建议为删除、修正、改名、隐藏。
- 前端状态以 `bc4a9fd8` 为取证基线；PR #74 合并后 PF2 的大部分内容即过期，届时以现场为准。