# COREONE 进销存「修流程做真」— 实现任务拆分（供多会话并行）

> **日期**：2026-07-02 | **性质**：把进销存（采购-销售-库存）基础流程 backlog 里**当前 master 真实还开着**的项，拆成互不碰文件的并行线，供多会话同步启动。**wave-1=修流程做真/补占位；wave-2=逐页前端重设计**（用户拍板"先修流程再重设计"）。
> **方法**：先用 ultracode Workflow 逐模块**核实 backlog 对当前 master 的真实状态**（`w1yoz1dxl`），筛掉已修/清单失真项，再定线。
> **配套必读**：`CLAUDE.md`、`.claude/rules/{coreone-guardrails,pr-governance}.md`、前端标准 `docs/COREONE-前端标准-流程质量设计文案UX-2026-06-27.md`、工作模型文档。

---

## 0 · 核实结论（先筛掉"清单说缺其实已做"的）

master tip `2bdbbee7`，**0 open PR**（进销存侧 origin 无在飞分支）。逐模块核实后：

| 模块 | 核实结果 | 处置 |
|---|---|---|
| 入库 inbound | IN-03/05/06（确认入库/扫码/批量导入）**代码里已修**，仅 `待办清单.md` 未更新 | ❌ 丢弃（勿重复劳动，仅需文档回填） |
| 退货给供应商 | 已完整（E2E 80 + 单测 31，无残留占位） | ❌ 丢弃 |
| 采购订单 | 菜单入口（`AppSidebar.tsx:82`）/路由/页面都在、无占位；MISS-09 详情弹窗=小 polish | ❌ 丢弃（小 polish 留后） |
| **调拨/退库/报废** | 三页缺筛选/统计/详情/导出/批量/排序；`transfers` 来源库位接收却不存 | ✅ **Lane C** |
| **预警** | 前端调 `/process`·`/ignore` 但后端只有 `/handle`（契约错位）；`/generate` 无入口；表单未透传 | ✅ **Lane E** |
| **库存/盘点** | DepletionTab/DepletedTab 空态白页；StocktakingAdjustModal 无 onSubmit 未接后端 | ✅ **Lane A** |
| **出库** | 排序硬编码 `created_at DESC`，表头点击无效 | ✅ **Lane B** |
| 主数据 | 分类编码展示/库位级联/物料 code 重生成（有失败测试 `materials.spec.ts:471`）+ **库位 code 规则需 PM 拍口径** | ⏸ **Lane D**（wave-2） |
| BOM | is_alternative 主辅料 UI/用量校验/物料清单可编辑 + **supportableSamples 需 PM 拍口径** | ⏸ **Lane F**（wave-2） |

---

## 1 · 通用约束（每条线都遵守，同成本侧拆分）

从最新 `origin/master` 出发（worktree）→ **工作模型 discussion-first**（先把方案摊给 PM 再写码）→ TDD 红绿 → 守 golden **¥13,152+¥27,870** 零回归（`cd 后端代码/server && npx vitest run` 全绿）→ 收尾 codex 异构独立复核。**本波是修流程/补占位，不是逐页重设计**（wave-2）：动界面遵现有设计系统、大改观感留 wave-2；全新页面/交互→走 **mockup 先行红线**。PII 纪律；PR body 按模板 + 更新 `pr-governance.md` 看板 + `session-log.md`；git 只 add 目标文件**别 `-A`**。**边界**：不碰对账/reconcile/收入侧/逐抗体成本（另有会话）。

---

## 2 · Wave-1 四条（互不碰文件，已分派 ultracode chip）

| 线 | 范围（已核实还开着） | 独占文件 | 冲突边界 | chip |
|---|---|---|---|---|
| **C 调拨/退库/报废**（M·mockup先行·**schema owner**） | 三页补 筛选/统计/快筛/详情弹窗(页内)/导出/批量/排序/URL同步；`transfers` from_location_id 持久化；returns/scraps created_at 索引 | `pages/{transfers,returns,scraps}/*` + `{transfers,returns,scraps}-v1.1.ts` | `api/inventory.ts` 只改 scrap/return/transfer 块；**本波唯一动 DatabaseManager schema 的线** | `task_9b1bf9c5` |
| **E 预警**（M） | `/process`·`/ignore` vs `/handle` 契约对齐；补 ignore；`/generate` 加入口；表单透传；空态 | `pages/alerts/*` + `alerts-v1.1.ts` | **不碰 DatabaseManager**（运行时 /generate 生成，别加 seed） | `task_f68fd867` |
| **A 库存/盘点**（S） | DepletionTab/DepletedTab 空态；StocktakingAdjustModal 接后端 wire 提交 | `pages/inventory/*` + `{inventory,stocktaking,depletion}-v1.1.ts` | `api/inventory.ts` **基准所有者**，只改 inventoryApi/depletionApi 块；不碰 schema | `task_9b77fcb3` |
| **B 出库排序**（S） | 后端 sortField/sortOrder 白名单；表头点击排序；功能补全清单.md 回填 | `pages/outbound/*` + `outbound-v1.1.ts` + `功能补全清单.md` | 不碰 schema | `task_0d75ee19` |

**共享文件消解**：`api/inventory.ts`（A↔C 各改各的导出块，A 为基准所有者）；`DatabaseManager.ts`（本波仅 C 动 schema，追加自己注释段，别的线不碰）；`App.tsx`/`AppSidebar.tsx` 本波不动（详情用页内 modal，无新路由）。

---

## 3 · Wave-2 两条（等 PM 拍口径，D 还等 C 的 schema 落地）

- **Lane D 主数据编码/库位层级**：CAT-CODE 展示 + LOC-HIER 级联 + MAT-CODE 重生成（含修 `materials.spec.ts:471`）。**待 PM 口径**：库位 code 生成规则（zone 前缀如 `A01-01` vs 固定 `LOC-`）。可先做不依赖规则的展示/后端修，再谈 code 规则；触 schema 需排在 C 之后。
- **Lane F BOM 做真**：is_alternative 主/辅料 UI（**注意 `is_alternative=辅料` 非"替代料"，见记忆**）+ main_item_id + 用量校验 + 物料清单可编辑 + import/export 做真。**待 PM 口径**：`supportableSamples` 字段语义/默认值。

---

## 4 · 待 PM 拍的口径（wave-2 起会话前或起会话中确认）

1. 库位 code 生成规则（Lane D）　2. BOM `supportableSamples` 语义/默认（Lane F）

*本拆分与成本侧拆分（`docs/COREONE-基础模块-实现任务拆分-2026-07-02.md`）同法。核实产出见 Workflow `w1yoz1dxl`。*
