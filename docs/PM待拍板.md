# PM 待拍板 · 单一决策队列

> **建**：2026-07-06（机制审查第 3 轮·提案 F 落地）| **owner**：PM + 当值 AI 会话 | **last-reviewed**：2026-07-06
> **为什么有这张表**：待 PM 拍板的事此前散落在 AI 记忆、各拆分文档、审计 PR、成本域收官页里，**没有一个收件箱** → PM 的决策带宽是最稀缺资源却没人替它排队。本表是**唯一入口**：新的待拍项一律登记到这里（成本域的细项保留在其收官页，本表只放指针）。
>
> **状态**：☐ 待拍 · ⏳ 讨论中 · ✅ 已拍（拍完写决定 + 日期 + 落地去向，不删行）。
> **怎么用**：PM 从上往下看，每项给个选择即可；AI 拿到决定后落地并把状态改 ✅。

---

## 甲 · 机制 / 流程类（源自 2026-07-06 机制审查）

| # | 一句话问题 | 背景 / 选项 | 推荐 | 状态 |
|---|---|---|---|---|
| M-1 | **e2e 回归网要不要重建可信度？** | 现状：PR 门只跑 2 个 spec；夜间全量每晚在跑但长期整体飘红（约 149 败）、失败信号无人消费 → 等于没有能报警的回归网（详见 `coreone-guardrails.md`「E2E 现状」）。选项：①分诊那 149 个失败、修到全绿再让它当门 ②只挑几条关键流程 spec 纳入 PR 门、其余暂放 ③维持现状但明确"e2e 仅供参考、不当回归网"。 | ②（先把最值钱的几条流程纳入 PR 门，成本可控），中期做① | ☐ |
| M-2 | **质疑关要不要按产出风险显式分档？** | 现规则=内容/文档产出都要过质疑关。已按 §6 矩阵软分档（碰钱走多镜头面板、纯文档走单轮）。问题：要不要写死明确档位（如"纯文案=自查清单免面板"）？还是保持"看 §6 矩阵判"？实证：45-agent 大报告反带 4 硬伤，堆 agent≠真度。 | 保持"按 §6 矩阵判"、不再加硬档位（够用且不僵化） | ☐ |
| M-3 | **session-log 要不要真归档（不只加读法机制）？** | 已加"读末尾 N 段 + 跳过失真首块"的读法机制。文件 1300+ 行仍在长。选项：①维持（读法机制够用）②把 2026-06 前的旧段移到 `docs/session-log-archive-*.md` 留指针。风险：多会话并行追加，大搬动易与其他会话的 append 冲突。 | ①维持（搬动风险 > 收益；读法机制已解决"每次从失真读起"） | ☐ |
| M-4 | **纯文档 PR 的 vitest（required）要不要也跳过？** | 已让 e2e 两侧 + backend-tests 的 push 侧对文档跳过。backend-tests 的 **PR 侧**故意没跳（否则 required check 挂起阻断合并）。要彻底跳需加 GitHub 官方"同名 no-op 占位 workflow"。成本：纯文档 PR 现多花约 1 分钟 vitest。 | 暂不做（1 分钟可接受，占位方案增复杂度）；除非文档 PR 频率变高 | ☐ |
| M-5 | **已合并 worktree 的回收要不要定成例行？** | 本次已回收可证明安全的（已合并+干净）子集。项目有 20+ 棵已合并树堆积、有"改错树"前科。选项：①每次收官批顺手回收 ②定期清 ③不管。 | ①（合 PR 后顺手 `git worktree remove` 已合并的自己那棵） | ☐ |

## 乙 · 成本域（细项在收官页，本表只放指针）

| # | 一句话问题 | 指针 | 状态 |
|---|---|---|---|
| C-* | 院级贡献毛利 P0 的 Q1–Q11 里仍有待打勾项（A 的 8 个阈值 / D 的 2 个前置 / E 的 RBAC 确认等） | 见 `docs/COREONE-P0-PM待拍清单-Q1toQ11收官-2026-07-04.md`（该页已是成本域的队列，本表不重抄，避免两处漂移） | ☐ 见收官页 |

## 丙 · 数据质量 / 政策类

| # | 一句话问题 | 背景 / 选项 | 推荐 | 状态 |
|---|---|---|---|---|
| P-1 | **病人隐私（PII）脱敏走哪种口径？** | 账实核对 A5：目前无脱敏代码、是政策未决。选项：①导入即脱敏（存哈希/掩码，系统内永不落明文 PII）②保留明文但严格 RBAC + 审计。医疗数据合规相关。 | ①（导入即脱敏，最省后患）——需 PM 确认业务上是否还需明文回查 | ☐ |
| P-2 | **「拆分口径高估约 2 倍」这个结论出没出门？**（非-P0 审计项 C 的分岔·**须指定 owner + 期限**，否则卡死在"不知道要不要披露"） | 拆分公式固定分母 `SPLIT_DIAG_FEE=105`（statement-revenue.ts）决定"范围内实验室 vs 范围外诊断桶"份额，是对外"高估约 2 倍"结论的唯一来源。已做工程档1（冻结常量+版本+纳入 caliberSignature+drift-guard·PR feat/split-const-freeze-C）。**待拍**=这结论进过财务口径/对外披露/对账谈判没有？①**没出门（纯内部 draft）**→ C 完结在档1，够了。②**已出门（进财务/对外）**→ 触发"更正/披露决策"：先决定要不要发更正、怎么向已收到旧结论方披露误差带，再谈是否档3逐客户可配（体检窗口内禁新增可配旋钮）。 | 先取证①/②（业务侧·非工程），未出门则维持档1 | ☐ |

## 丁 · 功能 backlog 待口径（有方向、缺 PM 拍口径才能开工）

| # | 一句话问题 | 背景 | 状态 |
|---|---|---|---|
| B-1 | 进销存 wave-2 **主数据（D）** 口径 | wave-1 四线已落 master；wave-2 的主数据模块要 PM 定口径再开工 | ☐ |
| B-2 | 进销存 wave-2 **BOM（F）** 口径 | 同上，BOM 缺 4 类维度待 PM 定 | ☐ |
| B-3 | ABC **personnel-efficiency（I-3）** 页：修还是删？ | 该页有幽灵接口缺陷；ABC 处置清单已定"做or删待 PM"（I-2/I-4/I-5 方向 PM 已拍、属实现 backlog，不在此列） | ☐ |

---

## 已拍存档（拍完移到这里，留决定 + 日期）

- （暂无——本队列 2026-07-06 新建。历史已拍决策见 `pr-governance.md` 看板各 PR 行 + 成本域 ADR/收官页。）

---

*配套 `工作模型-COREONE项目版`（头部指向本表）、`pr-governance.md`、成本域权威索引。本表只收"要 PM 拍板"的开放项；已拍/已实现的去各自权威文档，别在这里重复维护。*
